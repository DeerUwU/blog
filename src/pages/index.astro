---
import { Image } from 'astro:assets';
import { getCollection } from 'astro:content';
import BaseHead from '../components/BaseHead.astro';
import FormattedDate from '../components/FormattedDate.astro';
import Header from '../components/Header.astro';
import { SITE_TITLE } from '../consts';
import '../styles/text.css';


const posts = (await getCollection('blog')).sort(
	(a, b) => b.data.pubDate.valueOf() - a.data.pubDate.valueOf(),
);

---
<!doctype html>
<html lang="en">
	<head>
		<BaseHead title={SITE_TITLE}/>
		<style>
			body {
				height: 100vh;
			}
			main {
				width: 100%;
				height: -webkit-fill-available;
				height: 75vh;
				display: flex;
				flex-direction: row;
			}
			#main-left, #main-right {
				width: 50%;
				padding: 0px 20px;
				/* overflow: hidden; */
			}
			#main-left {
				border-right: var(--B-dark);
				
				padding-right: 20px;
				margin-right: 20px;
			}
			#main-right {
				max-height: fit-content;
				display: flex;
				flex-direction: column;
			}

			.entrylist-title {
				margin-bottom: 20px;
				/* text-decoration: underline; */
			}
			.entrylist {
				height: -webkit-fill-available;
			}

			/* blogpost list entries*/
			ul {
				display: flex;
				flex-wrap: wrap;
				gap: 1rem;
				list-style-type: none;
				margin: 0;
				padding: 0;
			}
			ul li {
				width: calc(100% - 1rem);
				background-color: var(--background-light);
				/* padding: 10px; */
				transition: 0.2s ease-in-out;
				transition-property: border-width;
			}
			ul li:hover {
				/* padding: 5px; */
				border-left: 5px solid var(--primary);
				transition: 0.2s ease-in-out;
				transition-property: border-width;
			}
			
			ul li * { /* every element inside the blog card */
				text-decoration: none;
				transition: 0.2s ease;
			}
			ul li:first-child {
				border-left: 5px solid var(--primary);
				background-image: linear-gradient(to right, var(--primary-darkest) , transparent);
			}
	
			ul li:first-child .title {
				/* font-size: 2.369rem; */
			}
			.thumbnail {
				margin-left: auto;
				border-radius: 0px;
			}
			ul li a {
				display: flex;
			}
			.cardText {
				padding: 10px;
			}
			.title {
				margin: 0;
				margin-bottom: 5px;
				color: rgb(var(--primary));
				line-height: 1;
			}
			.date {
				margin: 0;
				color: rgb(var(--gray));
			}
			ul li a:hover h4,
			ul li a:hover .date {
				color: rgb(var(--accent));
			}

		</style>
	</head>
	<body>
		<Header />
		<main>
			<div id="main-left">
				<h1>About me</h1>
				<p>
					Hey hello, hi!! I dont know how you found this place but uh congrats? This is some sort of blog or journal or whatever, a place where i keep track of my thoughts for anyone to read, really. I kinda want to stay anonymous for now. We're all strangers, after all.
					<br><br>
					I really want to fill this blog journal thingy with more personality and *style* in the future. I still have to learn working with Astro and maybe other webdev tools. Im trying out new things and so far i like it. But for now, this will suffice.
				</p>
				<div style="display: flex; flex-direction: row;">
					<img src="images/art/king rules.png" loading="lazy">
				</div>
			</div>
			<div id="main-right">
				<h1 class="entrylist-title">Offline's Journal</h1>
				<section class="entrylist scrollable">
					<ul>
						{
							posts.map((post) => (
								<li>
									<a href={`/blog/${post.id}/`}>
										<div class="cardText">
											<h2 class="title blogpost-title">{post.data.title}</h2>
											<p class="date">
												<FormattedDate date={post.data.pubDate} />
											</p>
										</div>
										
										{post.data.heroImage && (
											<Image class="thumbnail" height={80} src={post.data.heroImage} alt="" />
										)}
										
									</a>
								</li>
							))
						}
					</ul>
				</section>
			</div">
			

		</main>
		<!-- <Footer /> -->
	</body>
	
</html>
